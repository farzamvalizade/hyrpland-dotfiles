[server]
top_margin = 0.85

max_volume = 100

min_brightness = 5

show_percentage = true

keyboard_backlight = true

playerctl_format = "{artist} - {title}"

[client]
# This section remains empty for now; the OSD is controlled via the server settings